describe('user - selectors', () => {
  it('userSelector', () => {
    const state = {
      user: {
        token: null,
        errorMessage: 'Error message',
      },
    };
  });

  it('tokenSelector', () => {});

  it('userErrorMessageSelector', () => {});
});
